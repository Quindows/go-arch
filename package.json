{
  "name": "depth",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass -w scss:css",
    "minify": "sass -w scss:css --style=compressed"
  },
  "author": "QuintinBlume",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.60.0"
  }
}